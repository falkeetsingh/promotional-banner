<style>
  .promo-banner {
    background: linear-gradient(90deg, #f9d423, #f9e200, #f9d423);
    color: #333;
    padding: 15px 20px;
    text-align: center;
    font-weight: 700;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    font-size: 1.4rem;
    letter-spacing: 1.5px;
    box-shadow: 0 4px 10px rgba(249, 226, 0, 0.5);
    border-radius: 8px;
    position: relative;
    overflow: hidden;
    cursor: default;
    user-select: none;
  }

  /* Animated confetti circles */
  .confetti {
    position: absolute;
    top: 50%;
    width: 12px;
    height: 12px;
    background-color: #ff5722;
    border-radius: 50%;
    animation: floatUp 3s ease-in-out infinite;
    opacity: 0.8;
  }
  .confetti:nth-child(1) {
    left: 10%;
    animation-delay: 0s;
    background-color: #ff5722;
  }
  .confetti:nth-child(2) {
    left: 30%;
    animation-delay: 1s;
    background-color: #ffeb3b;
  }
  .confetti:nth-child(3) {
    left: 50%;
    animation-delay: 2s;
    background-color: #4caf50;
  }
  .confetti:nth-child(4) {
    left: 70%;
    animation-delay: 0.5s;
    background-color: #2196f3;
  }
  .confetti:nth-child(5) {
    left: 90%;
    animation-delay: 1.5s;
    background-color: #e91e63;
  }

  @keyframes floatUp {
    0% {
      transform: translateY(15px) scale(1);
      opacity: 0.8;
    }
    50% {
      opacity: 1;
      transform: translateY(-15px) scale(1.2);
    }
    100% {
      transform: translateY(15px) scale(1);
      opacity: 0.8;
    }
  }

  /* Bounce animation for the banner text */
  @keyframes bounce {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-6px);
    }
  }

  .promo-banner span {
    display: inline-block;
    animation: bounce 2.5s ease-in-out infinite;
  }
</style>

<div class="promo-banner" role="banner" aria-live="polite" aria-atomic="true">
  <span>🎉 Free Shipping on All Orders! 🎉</span>
  <!-- Confetti dots -->
  <div class="confetti"></div>
  <div class="confetti"></div>
  <div class="confetti"></div>
  <div class="confetti"></div>
  <div class="confetti"></div>
</div>

{% schema %}
{
  "name": "Promotional Banner",
  "target": "section",
  "settings": []
}
{% endschema %}
